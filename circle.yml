## Customize the test machine
machine:

  timezone:
    Europe/Paris # Set the timezone

  java:
    version: openjdk8

test:
  override:
#     - mvn clean
#    - mvn validate
#    - mvn compile
    - mvn test

## Customize deployment commands
general:
  branches:
    only:
      - master
